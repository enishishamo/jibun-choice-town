# design/v2/generated — 生成候補

Claude / Codex / Image Generation 等が作った候補画像。**採用決定ではない**。

- ファイル名は `<対象>-<日付>-<連番>.png`（例: `companion-front-2026-10-01-01.png`）
- 生成に使ったプロンプト・元 Reference・Art QA HARD GATES の自己チェック結果を
  同名の `.md` に残す
- human approval を経て `master/` へ、不採用は `rejected/` へ移す
- 有料 API は使わない（`CLAUDE.md` §8）

現在: 空（今回のタスクでは画像生成を開始していない）。
