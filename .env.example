# 画像生成パイプライン用の環境変数の見本。
# 実際のキーは「.env.local」（.gitignore の *.local で除外済み＝コミットされない）に書くこと。
#
#   cp .env.example .env.local
#   .env.local を開いて sk-... を貼り付ける
#
# キーは https://platform.openai.com/api-keys で発行。
# このファイル（.env.example）には絶対に本物のキーを書かない。
OPENAI_API_KEY=
