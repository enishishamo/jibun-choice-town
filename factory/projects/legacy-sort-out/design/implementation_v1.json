{
  "component_files": [
    "src/q1/RecycleGame.tsx"
  ],
  "logic_module": "src/q1/sortOutLogic.ts",
  "qa_harness": "factory/harness/gameplay-qa-sort-out.mjs",
  "spec_version": 1,
  "art_version_or_none": "none (art_brief v1: no_art_required=true; existing recycle_sorting.jpg/food_waste.jpg/compost_to_farm.jpg reused as-is, no new assets; schoolLunch.ts's recycle-lunch entry's tools: [] emptied since the custom component renders its own tool UI, matching the delay_recover/hotel_receive precedent)",
  "d_preserved_statement": "sortOutLogic.ts mirrors factory/projects/legacy-sort-out/design/design-sim.mjs exactly (PROPERTIES/TOOLS/CORRECT_TOOL/ITEMS/newSession/sessionWin structurally identical -- verified by factory/harness/gameplay-qa-sort-out.mjs's pure rule-level and strategy-simulation checks, 25/25 passing with numbers matching design-sim-result.json). RecycleGame.tsx implements the exact D from game_spec_v1.json: 3 item cards sharing a neutral appearance, each independently and randomly assigned one of 3 properties, opened via a disclosure gate before a tool can be selected, a two-step select-then-apply single commit per item (no retry), and a Gate G non-scored reflection re-presenting all 3 items' observations read-only on any mismatch before Gate H's distinct onPartialComplete outcome.",
  "browser_verification": "Manually played through both outcomes in the dev preview (no automated E2E harness for this repo): (1) full success path (all 3 items' properties read correctly, matching tools applied) reached the 'done' branch with the 3つとも、性質に合う道具で正しく仕分けられた！ result and the compost_to_farm resolution scene; (2) partial-failure path (1 of 3 items deliberately mismatched) reached the 'reflecting' branch, re-presented all 3 items' observations and the child's actual prior tool choice read-only, required a full reflection pick set before enabling 先へ進む, and on completion showed the shell's honest partial-outcome chip (けっかを送ったけど、まだ全部はつながっていないかも) instead of the success framing, then the correct discoveryEcho/job-reveal copy with no stale '網' reference. No console errors in either path. Verified the mobile (375px) layout for both the closed 3-card list and an expanded card's tool-selection UI -- no overflow, uniform card heights.",
  "regression_note": "Fixed a pre-existing content/code mismatch unrelated to this game's own exploit but discovered during research: schoolLunch.ts's tools array already listed a 風力選別 (wind, 💨) tool that the OLD RecycleGame.tsx never actually implemented (it used a 網/net-based tool instead) -- this rebuild's adoption of wind separation as the 3rd real tool resolves that inconsistency as a side effect, not an independent fix requiring separate review."
}
