{
  "first_5_seconds": "Verified live in the Browser pane at 375x812: entering the chapter shows the task-bar goal line, 4 diagnosis candidate cards and 6 evidence cards immediately, all interactive with no explanatory paragraph — matches first_5_seconds_v6.json/no_manual_requirements.",
  "touch_targets": "join-main (evidence cards) min-height 44px, dx-commit (diagnosis commit button) min-height 44px, dx-more/join-more disclosure toggles kept at the existing 38px precedent already used elsewhere in this app (join-more/task-sub disclosure controls) — all >= the 44px requirement for primary actions.",
  "mobile_375": "Measured live via javascript_tool at viewport 375x812: document.scrollHeight 819px vs innerHeight 812px (7px residual, effectively fits without scroll) for the initial unplayed state — 10 cards + task-bar + budgeted submit button all visible without horizontal scroll. Screenshots taken confirm no clipped or hidden content.",
  "direct_manipulation": "Diagnosis commit and evidence selection are both direct taps on the card itself (or its commit button) with immediate visual feedback (border/background color change, checkmark) — verified live via click-and-screenshot in the Browser pane.",
  "reaction_timing": "State updates (commit highlight, evidence toggle, submit feedback) render synchronously on tap with no artificial delay, consistent with every other Q1 game in this app.",
  "c_to_d": "Verified: the 6 evidence cards (話/診察/呼吸状態/検査/画像/循環) and 4 diagnosis candidates' typical-pattern text (fact_sheet.candidate_reference_cards, opened via '? どんな病気') are the only information available; the child must compare them and commit — the component never auto-suggests or highlights a correct pairing (gameplay-qa-clue-join.mjs: 'CORRECT_DIAGNOSIS is never used before the success screen').",
  "consequence": "A winning submission (pneumonia + an evidence set that is a superset of {lab,xray} and subset of {lab,xray,talk,exam}) shows a distinct success screen contrasting the 3 other candidates and explicitly notes why spo2/bp were not decisive; a losing submission returns one flat, uninformative sentence with no diagnosis/evidence-specific hint (verified live and by gameplay-qa-clue-join.mjs's answer-leak/random-guess-rate checks).",
  "retry": "Within budget (2 attempts), a wrong submission decrements the counter shown on the submit button ('あと1回') and lets the child freely re-commit/re-select with the SAME shuffled order (no reshuffle mid-session) — verified live. After budget exhaustion, the honest partial screen offers both 'もう一度考える' (resets budget to 2, reshuffles orders, verified live) and '先へ進む' (calls onPartialComplete, verified live — Job Reveal still fires).",
  "answer_leak": "gameplay-qa-clue-join.mjs verifies: no diagnosis candidate's read-only pattern text contains a verdict phrase or names another candidate; CORRECT_DIAGNOSIS is never referenced before the outcome is decided; the per-session shuffle is id-based (scoring never touches display position). Live testing confirmed the '?' pattern disclosure never reveals or implies which candidate is correct.",
  "brute_force": "gameplay-qa-clue-join.mjs: select_all / has-number-only / no-number-only heuristics all win 0% of simulated cases (N=8000); blind random evidence+diagnosis stays at 3.06% (matches design-sim.mjs's disclosed theoretical baseline); the r2-class fixed-position exploit (first-displayed candidate + evidence positions 1,2,4,5) matches the chance baselines 1/60 (single try) and 1/30 (2-try cycled), i.e. no better than blind guessing once shuffled.",
  "visual_affordance": "Committed diagnosis card and selected evidence cards both get a clear orange border/background change plus a checkmark; the disabled submit button (before any diagnosis is committed) is visually dimmed via the existing btn:disabled opacity rule — verified live via screenshot.",
  "no_manual": "No instructional/how-to-play text anywhere in the screen; card meaning is discoverable via tap and the '?' disclosure only, matching no_manual_exploit_check_v9.json and every sibling Q1 game's convention.",
  "job_reveal": "onComplete/onPartialComplete both route to the existing shared Q1 shell, which renders medical.ts's unchanged med-diagnose discoveryEcho/resolution/seed-picker — verified live end to end (success path reached the '🎉 肺で炎症が起きている可能性が高い' discoveryEcho screen; ch4 flipped from active to done and ch5 med-pharm unlocked).",
  "pass": true,
  "evidence": {
    "gameplay_qa": "node factory/harness/gameplay-qa-clue-join.mjs -> 31 passed, 0 failed",
    "self_test": "node factory/harness/q1-factory-selftest.mjs -> 41/41 passed",
    "typecheck": "npx tsc --noEmit -p . -> no output (clean)",
    "build": "npm run build -> succeeded",
    "lint": "npm run lint -> no new errors/warnings introduced by DiagnoseGame.tsx, clueJoinLogic.ts, or gameplay-qa-clue-join.mjs",
    "live_browser_verification": "Browser pane at 375x812: seeded localStorage progress (med-doctor/med-lab/med-radio completed) to reach ch4, played a full success run (committed pneumonia + lab/xray, confirmed success screen and Job Reveal/discoveryEcho and ch4->done/ch5 unlock), and a full 2-attempt partial run (committed a wrong candidate twice, confirmed honest partial screen, confirmed 'もう一度考える' resets budget/reshuffles, confirmed the inline '?' pattern disclosure keeps all other controls reachable)."
  }
}
