{
  "component_files": ["src/q1/DiagnoseGame.tsx"],
  "logic_module": "src/q1/clueJoinLogic.ts",
  "qa_harness": "factory/harness/gameplay-qa-clue-join.mjs",
  "spec_version": 1,
  "art_version_or_none": "none — art_brief v1 declares no_art_required: true (reuses public/assets/medical/er_exam.jpg, no new asset)",
  "d_preserved_statement": "The child still performs the real judgment: committing to one of 4 diagnosis candidates via an explicit 'これだと思う' action (never pre-filled or suggested by the system) and independently selecting which of the 6 gathered findings are the actual decisive evidence for it. Both are scored together only after the child's own submission; the system never reveals the correct diagnosis or evidence set before that point (verified by gameplay-qa-clue-join.mjs's CORRECT_DIAGNOSIS pre-commit-hint and answer-leak checks). This mirrors the real differential-diagnosis structure preserved through c_compression/game_translations: match findings against candidate patterns, commit to the best-fitting one, and be able to say why.",
  "revision_note": "v2 (implementation review r1 FAIL 74, 2 HIGH, 0 blockers, repair — repair_count 2/2, downstream repair per q1-pipeline.mjs 'fail' rules, no design-stage budget touched, IMPLEMENTATION_CHANGED_D routed to IMPLEMENTATION only): (1) CLINICAL_MODEL_OVERSTATED — src/q1/DiagnoseGame.tsx's success screen no longer labels the 3 alternative diagnoses with absolute exclusion ('○○ではない'); it now shows a '○○との比較' heading with each candidate's own tendency-worded pattern text followed by a single comparative sentence ('今回そろった所見は、これよりも肺炎の典型像によく合う。'), preserving fact_sheet_v12's non-absolute framing while still satisfying game_spec's requirement to contrast the 3 alternatives. (2) DISCLOSURE_TOUCH_TARGET_TOO_SMALL — the diagnosis-candidate '?' disclosure (src/index.css .dx-more) is now a real 38x38px circular button (was 0 padding/no min-height, ~18px effective), restructured into a single dx-head row (dx-name + the '?' circle) so overall card height did not grow; re-measured live at 375x812: document.scrollHeight now exactly 812px (was 819px pre-fix), i.e. fits with zero overflow."
}
