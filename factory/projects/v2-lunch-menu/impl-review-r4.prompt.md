# Independent implementation review — Ver.2 給食 WORLD vertical slice (r4)

Round 3 (`impl-review-r3.result.json`, FAIL 58) findings addressed: (blocker) the "attributes drawn on the dish" contract was STALE — by the 2026-09-21 Human/GPT spec the dish art is a pure visual asset and the attributes are made visible in the UI layer; the header comment of lunchMenuLogic.ts now states this and the harness checks a rendered cue per rule (do NOT ask for attribute overlays on the dish art — that would violate the approved spec). (high) ver1-freeze-check now excludes public/assets/v2/**. (medium) relatedSet capped at 2 with an exhaustive assertion; harness uses a tmpdir Vite cache and is exposed as `npm run qa:v2-lunch`. (low) index.css header fixed.

SINCE r3 the PLAY UI was rebuilt after a Design-Owner Visual Review (`factory/projects/v2-lunch-menu/experience-design-proposal.md` §3'''): the status layer is no longer a row of abstract marks. It is (a) three baskets rendered in front of the tray (`StatusLayer` → `.lmp-baskets`, one per colour group, `data-group`, `data-cue`) that show the bits each placed dish contributes (`groupCounts`, `BASKET_SPOTS`, bits ≥ GROUP_MAX spill over the rim, < GROUP_MIN shows an empty echo) and (b) rule cues placed ON the tray at the related dish's slot (`.lmp-mark.<rule>` positioned with `SLOT_POS`/`MILK_POS`/`GHOST_POS`; salt specks, oil puddle+drips under the dish via z-index layering, a bit hopping between the two duplicate dishes via CSS vars `--x0/--y0/--x1/--y1`, a ghost ring for missing_role, plus the matching counter row floating via `.lmp-shelf.wanted`). Bits fly dish→basket (`launchGrains`) for every dish on the first full tray (after the baskets' entrance animation) and for each dish added afterwards. Tapping a counter dish now animates a flying clone (`fly`/`flyGo`/`arriving`, FLY_MS) from the counter to the slot, then `pop` + `sparks`. The counter is four role rows (`COUNTER_ROLES`, serving order) on one surface; a dish on the tray shows as a used spot (`.lmp-used`), an EVENT-unavailable dish stays visible grey with a badge. StatusLayer is rendered BEFORE `.lmp-slots` inside the tray so puddles stack under the dishes and marks above them.

Re-review the SAME scope adversarially, verify each earlier fix, and look hard at the new code: timers/refs (`fx`, `basketsShown`, `prevTray`, `arriving`, `fly`) under rapid taps, StrictMode double-effects, removal during a flight, EVENT firing while a clone is in flight, `launchGrains` reading rects during animations, StatusLayer purity for the SSR harness (it renders it with `renderToStaticMarkup` — any DOM/window access there breaks the harness), aria correctness of the new elements, DESIGN LOCK (strings only in copy.ts, palette v1 tokens only, TEMP markers), and whether the harness (`gameplay-qa-v2-lunch-menu.mjs`, 26 checks) still independently proves the contract (every rule has a rendered cue, no score rendered, DishFace attribute-free, ≤2 related dishes, exhaustive EVENT recovery).

Scope: `src/v2/lunch/**`, `src/v2/state/progress.ts`, `src/v2/App.tsx`, `src/v2/copy.ts`, `src/v2/index.css`, `factory/harness/gameplay-qa-v2-lunch-menu.mjs`, `factory/harness/ver1-freeze-check.mjs`, `factory/harness/v2-lunch-shots.mjs`. Ver.1 (`src/screens`, `src/q1`, `src/state`) is frozen; `src/v2` must not import it.

Binding rules: `docs/jibun-choice-v2/PRODUCT_PRINCIPLES.md`, `GAME_DESIGN_RULES.md` §1, `DESIGN_OWNERSHIP.md` §2 (all user-visible strings incl. aria in `src/v2/lunch/copy.ts` / `src/v2/copy.ts`; placeholders marked TEMP_IMPLEMENTATION_ONLY; only palette v1 tokens `--v2-*` or `color-mix()` of them), `VISUAL_TONE.md` §3. Contract in `lunchMenuLogic.ts` header comment: milk fixed slot (V-A1), EVENT only after first result + ≥1 re-arrangement + full tray, CLEAR = child's commit with no score threshold, every rule reads only VISIBLE_ATTRIBUTES. Design constraints from the Visual Review: no total score, no explanation text, no new characters, no filler decoration, no right/wrong display, selected dishes never faded.

Axes (0–100 each): correctness (state machine, timers, StrictMode double-effects, stuck states, persistence isolation), design-rule compliance, QA coverage. Try to construct concrete breaking sequences; cite file:line.

OUTPUT FORMAT (mandatory): your ENTIRE final message must be ONE JSON object, no prose, no code fences, exactly this shape:
{"verdict":"PASS|FAIL|HUMAN_REQUIRED","score":0-100,"blockers":[string],"high":[string],"medium":[string],"low":[string],"evidence":[string],"recommended_actions":[string]}
Each finding string: "<file>:<line> — <title> — <why> — <fix>". blockers/high non-empty ⇒ verdict FAIL.
