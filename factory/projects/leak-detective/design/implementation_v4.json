{
  "component_files": [
    "src/q1/LeakTraceGame.tsx",
    "src/q1/registry.ts (gameType leak_trace)",
    "src/data/content/extremeHeat.ts (incident leak-night, profession leak-detective, experience leak-heat, lensSummary row)"
  ],
  "logic_module": "src/q1/leakLogic.ts",
  "qa_harness": "factory/harness/gameplay-qa-leak.mjs (52 checks: mirrors every design-sim v9 strategy incl. loudest-steady and the flow-only reopen→listen→report adaptation (32.5%, parity with the sim), 4 gate sub-tests incl. 'a silent listen does not unlock', deep-equality valve check, source-level publicView boundary check, silent-state regressions (dedicated level 0 / silent; silent record dropped on explicit and implicit reopen with re-listen allowed and no refund; non-silent records survive; a silent reading cannot be reported), the crew-restores-supply check (report while closed → closed null, silent records gone, hit and miss), an all-plays invariant that a silent reading exists only on the currently closed segment, source checks that the only waveform strip lives in the non-silent branch, that the live blink / latest-reading row are gated on the segment being open, and that the component announces the crew restoring supply)",
  "spec_version": 3,
  "art_version_or_none": "art_production v3 (same asset as v1) — public/assets/heat/place-leak.png (codex_imagegen, art-qa PASS iter 1, min 76, SERIES_STYLE_MATCH 88, CHARACTER_SERIES_MATCH 100); profession image = inline SVG hero placeholder (Claude); all in-game visuals CSS/SVG (Claude)",
  "d_preserved_statement": "The child performs D exactly as designed (t1c-night-listening-isolation, design review r9 PASS 84): chooses which valve to close and reads the meter (segment isolation; one valve at a time; 2 closes per night; explicit free reopen by tapping the closed lid 『▲開ける（0回）』), reopens before listening (a closed segment is the dedicated silent state level 0 / silent — text only, no bars, no waveform element, no continuity word, no window blink), places the child's own 『調べる』 hypothesis focus, chooses which points to listen at and reads level/continuity from the record strip, excludes the intermittent (house-usage) sound, and decides which heard point to REPORT. The component reads state only through publicView()/readingAt()/restartSameCase() (verified at source level by the QA harness); no UI reaction depends on correctness (valve flash identical for every segment; segment focus is child-driven only; the house-window blink is driven by the reading's continuity and only while that segment is open, never by the leak). Human Decision hd-2 (Gate G): the second report is enabled only by a NEW NON-silent listen taken after the miss (leakLogic.listen sets unlocked only for a non-silent reading); a report is grounded only in a heard, currently valid, non-silent point (reportBlocked: not_heard / silent_not_evidence / already_missed); silent records invalidated by a reopen — the child's explicit reopen, the implicit reopen when another valve is closed, or the CREW's reopen when a report is made while a valve is closed — leave the state, the timeline and the selection together. state_table.report_rule 'the crew restores supply, then digs' is implemented: report() sets closed → null and drops that segment's silent records; the UI logs 『修理班が弁◯を開けて水を戻した → 修理班が掘る』, shows every lid open during the dig and says 『修理班が水を戻して掘っている…』 (impl review r3 HIGH fix). The record panel is read-only. The repair crew restores supply and digs (surveyor pinpoints & reports), matching scope_core_v2/ae_v3.",
  "state_table_conformance": "leakLogic.ts constants mirror state_table.json: 3 segments x 6 points, budgets valve 2 (closes) / listens 5 / reports 2, flow base 0.2 + leak 2.0 − 0.05 per closed segment (1 decimal), sound level = max(1, 5 − distance) steady on the leak segment, level 1 steady elsewhere, house point level 4 intermittent, closed segment = { level: 0, continuity: 'silent' } at every point (isolation_rule), silent_record_lifetime (dropped on explicit/implicit/crew reopen, re-listen costs a listen, no refund), valve_model (close = budgeted; reopen = free explicit tap; closing another valve implicitly reopens), think_again_gate = non-silent new listen after the previous report only, report_rule (heard / currently valid / non-silent / not already missed; crew restores supply before digging). gameplay-qa-leak.mjs reproduces every design-sim v9 verdict flag against the shipped module (legitimate 306/306; random listen-and-report 10.7%; flow-less listening 26.2%; flow-only reopen→listen→report 32.5%; valves-ignored segment guess 23.0%; forgets-reopen 33.2%; gate 4 sub-tests).",
  "commands_run": [
    "node factory/projects/leak-detective/design/design-sim.mjs -> 8/8 verdict flags true, 306/306 solvable, gate 200/200/200/200",
    "node factory/harness/gameplay-qa-leak.mjs -> 52 passed, 0 failed (flow-only 0.325)",
    "npm run build -> ✓ built",
    "npm run lint -> no findings in leak files"
  ],
  "human_decision_domains": [],
  "repair_note": "Implementation review r3 (FAIL 68) repair (iteration 3, implementation repair 2/2): (HIGH) report() now restores supply — closed → null and that segment's silent records dropped — and the UI announces it in the timeline, during the dig (『修理班が水を戻して掘っている…』) and by showing every lid open; (MEDIUM) game_spec v3 unified to level 0 / silent (two leftover 'level 1 steady' fields) and states the crew's reopen; (MEDIUM) QA closed-segment-report check now verifies closed null + silent records gone for hit and miss, plus a source check for the announcement; (MEDIUM) the harness flow-only strategy reopens before listening (parity with design-sim: 32.5% both) and the evidence documents are regenerated from the harness; (LOW) the reaction row shows the silent text for a silent listen (condition: silent OR the heard segment is open) so the branch is reachable. Layout: compact miss note (one line) and records strip 34px so the night screen stays at 812/812 at 375x812 even with the note and the reaction row shown.",
  "revision_note": "v4 (after implementation review r3): crew restores supply before digging; doc/QA/evidence parity; reachable silent reaction row; no-scroll layout with the miss note."
}
