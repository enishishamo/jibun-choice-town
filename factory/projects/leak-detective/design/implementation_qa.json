{
  "first_5_seconds": "PASS — live at 375x812 (Vite dev server, Browser pane): night map with 3 segments, valves, 18 glowing points, gauge needle at 2.2 with '正常ならここ' mark, report card in the task bar, budgets row, empty record strip, report button disabled with '報告したら戻せない、あと2回'. No instruction line, no wet patch, no leak mark. Screenshot captured in-session.",
  "touch_targets": "PASS — re-measured in the Browser pane at 375x812 after the implementation repair: listening points and valve lids both use r=28 hit circles in the 400-unit viewBox = 44.7px (21 circles, DOM-measured); the segment-focus controls are now HTML buttons under the map with min-height 44px (measured 44/44/44px). Report button is a full-width .btn.big.",
  "mobile_375": "PASS — the whole night screen fits without page scroll (scrollHeight 812 = innerHeight 812, re-measured after the layout fix). Point-number labels sit above/right of the points so the segment labels no longer overlap them; the record strip scrolls within 74px.",
  "direct_manipulation": "PASS — every action is a direct tap on the world object: valve lid (close; closing another valve implicitly reopens the previous one — tapping the closed lid only shows a note, there is no free reopen action), segment-focus buttons (the hypothesis of the child), listening point (listen; heard point = select), report button.",
  "reaction_timing": "PASS — valve: needle transition 0.8s + segment line flash 0.7s; listen: waveform strip appears immediately (steady loop / intermittent 1.1s burst + house window blink); report: 1.4s digging animation (🚧) then hit/miss.",
  "c_to_d": "PASS — live run: closing valve A left the meter at 2.2, closing valve B dropped it to 0.2 (segment identified by reading C); listening B2=4, B5=1, B3=3, B4=2, B1=5 gave the gradient toward point 1. gameplay-qa-leak.mjs: informed play 100%, flow-less/flow-only/segment-guess strategies 29-36%.",
  "consequence": "PASS — hit: '当たり！ 漏水点を特定した', crew digs B1, water sprays from the crack, dawn: gauge back to 0.2 ('正常ならここ'), resolution chip '🎉 明け方 漏れは、止まった'. Miss: '乾いた管だった……埋め戻し', ✕ mark on the map, needle stays 2.2. Partial (no listens left after a miss): '今夜は特定できなかった', damp road, needle 2.2, '次の夜へ持ち越し'. All three observed live.",
  "retry": "PASS — after a miss the report button reads 新しく聴いてから（報告はまだ） and is disabled (live note: 乾いた管だった……埋め戻し); a new listen re-enabled it and the correct second report hit (live run after the repair: A→2.2, B→0.2, B2=4, B5=4, B3=5, wrong report B2 → locked → listen B1 → report B3 → 当たり). Record panel is read-only (openRecords is the identity). The specific one-line hint is gated on the second FAILED NIGHT (failedNights >= 2) or two misses in one night, never on a first miss.",
  "answer_leak": "PASS — LeakTraceGame.tsx reads game state only through publicView()/readingAt()/restartSameCase(); the QA harness asserts at source level that the component never reads s.c, heard(s), .leak or .house properties (FLOW.leak constant excepted) and uses revealLeak exactly once (post-hit dawn screen), and at logic level that the public view after closing the leak segment vs another segment differs ONLY in the meter number (deep equality with flow/closed stripped). Valve flash is the same for every segment; segment dimming follows only the focus toggle; house-window blink follows the reading continuity.",
  "brute_force": "PASS — budgets enforced by leakLogic (3rd valve op refused, 6th listen refused, duplicate listen refused, report of an unheard point refused, 2 reports max, second report locked without new evidence). gameplay-qa-leak.mjs: random reporting 0.0%, flow-only two reports 33.3%.",
  "visual_affordance": "PASS — glowing 🎧 points read as listen-here; valve lids read as close; 区間◯を調べる labels read as hypothesis buttons; heard points show ▮ bars, missed points show ✕; the last reading is echoed as a waveform strip with ずっと/とぎれる; record strip scrolls (max-height 74px) so all 9 possible entries stay reachable.",
  "no_manual": "PASS — no explanation screen or instruction line; the first touch is inferable from the glowing points / valve lids / trembling needle; continuity is learned from the waveform + window blink, not text.",
  "job_reveal": "PASS — discovery screen: name 水道の漏水を見つける仕事（漏水調査員）, discoveryEcho is outcome-neutral (「漏水点はどこか」を考えたよね) so a partial run is never told it identified the leak; 📖 しごと図鑑に追加された！; 6 seed chips. Q2 cards sourced from fact_sheet.json.",
  "pass": true,
  "evidence": [
    "factory/harness/gameplay-qa-leak.mjs run: 30 passed, 0 failed",
    "npm run build ✓, npm run lint (no findings in leak files)",
    "Browser pane session at 375x812 on the Vite dev server: area hotspot, intro (cover crop of place-leak.png), night screen, valve isolation A→2.2/B→0.2, listens B2/B5/B3/B4/B1, miss→locked→new listen→hit, partial screen, dawn screen, discovery screen — DOM reads + screenshots captured in-session; read_console_messages: no errors",
    "factory/projects/leak-detective/art-requests/place-leak.qa.json (art-qa PASS)",
    "factory/projects/leak-detective/design/design-review-r4.result.json (design PASS 84)",
    "Implementation repair (impl review r1 FAIL 72): live re-verification at 375x812 after the repair — 21 x 44.7px hit circles, focus buttons 44px, no page scroll; full path A→2.2 / B→0.2 / gradient / wrong report locked / new listen / hit; gameplay-qa-leak.mjs 38 checks incl. source-level publicView boundary and deep-equality valve check"
  ],
  "notes": "Console note: 23 useGame-must-be-used-inside-GameStateProvider errors were observed in the dev console during this session; they appeared while Vite HMR re-evaluated src/data/content/extremeHeat.ts and src/q1/registry.ts under a mounted Q1Screen, and did NOT recur on a fresh navigation + full play-through (count unchanged at 23). They are HMR artifacts of editing data modules live, not a runtime defect; the production build (npm run build ✓) is what deploys.",
  "human_decision_domains": []
}
