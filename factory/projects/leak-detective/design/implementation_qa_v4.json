{
  "first_5_seconds": "PASS — live at 375x812 (Vite dev server, Browser pane, 2026-09-09): night map with 3 segments, valve lids, 18 glowing points, gauge needle at 2.2 with '正常ならここ' mark, report card in the task bar, budgets row 『🔧 閉める あと2 / 🎧 聴く 5 / 📣 報告 2』, empty record strip, report button disabled with '聴いた点をえらんで報告（報告したら戻せない、あと2回）'. No instruction line, no wet patch, no leak mark.",
  "touch_targets": "PASS — listening points and valve lids use r=28 hit circles in the 400-unit viewBox = 44.7px at 375px (21 circles, DOM-measured in earlier rounds; unchanged); the closed lid's 『▲開ける（0回）』 label is SVG fontSize 14; segment-focus controls are HTML buttons with min-height 44px (measured 44); report button is a full-width .btn.big (60px).",
  "mobile_375": "PASS — the night screen fits without page scroll in every state re-measured live after the r3 repair: initial 812/812, after a silent listen 812/812, with the live reaction row 812/812, after reopen 812/812, after a MISS with the note AND the reaction row shown 812/812 (records strip 34px, compact one-line note 26px), hit/dawn screen 812/812. The discovery (job reveal) screen scrolls by design.",
  "direct_manipulation": "PASS — every action is a direct tap on the world object: valve lid (close, budgeted; tapping the closed lid reopens for free — 『▲開ける（0回）』; closing another valve implicitly reopens the previous one), segment-focus buttons (the hypothesis of the child), listening point (listen; heard point = select; a point whose silent record was dropped by any reopen shows as unheard again), report button.",
  "reaction_timing": "PASS — valve: needle transition 0.8s + segment line flash 0.7s; listen on an open segment: waveform strip appears immediately (steady loop / intermittent 1.1s burst + house window blink); listen on a closed segment: immediate reaction row with only the text 『静か（水が止まっている）』 (no strip element, no bars, no motion); report: the crew's reopen (if any) is logged immediately, 1.4s digging animation (🚧, 『修理班が水を戻して掘っている…』, every lid open) then hit/miss.",
  "c_to_d": "PASS — live run 2026-09-09: closing valve A left the meter at 2.2, closing valve B dropped it to 0.2 (segment identified by reading C); reopen B (free); listening B1=3, B6=2, B2=4, B3=5, B4=4 gave the gradient toward point 3; report B3 → hit on the first report. gameplay-qa-leak.mjs 52/52: informed play 100%; random listen-and-report 10.7%; flow-less listening 26.2%; flow-only reopen→listen→report 32.5% (parity with design-sim v9); segment guess 23.0%; forgets-reopen 33.2%.",
  "consequence": "PASS — hit (live): '1回目で当たり！ 漏水点を特定した', 修理班が 区間B（東側） の3番の下を掘ると、管の割れ目から水が噴き出していた → 修理 → dawn: gauge back to 0.2 ('正常ならここ'), '🎉 明け方 漏れは、止まった'. Miss (live, r3 fix): a valid record on a CLOSED segment reported → timeline 『📣 C1 を報告 → 🔧 修理班が弁Aを開けて水を戻した → 修理班が掘る』, closed lids 0 during the dig, then '乾いた管だった……埋め戻し。新しく聴いてから。', ✕ mark, needle stays high, button '新しく聴いてから（報告はまだ）'. Partial (logic unchanged: report → partial when no reports or no listens remain): '今夜は特定できなかった', damp road, '次の夜へ持ち越し'.",
  "retry": "PASS — Gate G live: wrong report → button '新しく聴いてから（報告はまだ）' disabled; a silent listen on a closed segment → still locked (listens decrement); a non-silent listen → unlocked. A point recorded as silent cannot be reported: '静かな点は根拠にならない（水を戻して聴く）' disabled. A record heard on an open segment stays reportable after that segment is closed; the crew then restores supply before digging (live: lids 0, timeline line). Record panel is read-only (openRecords is the identity). Silent-record lifetime live: after the child's explicit reopen, the implicit reopen (closing another valve) and the crew's reopen at report time, the 『静か』 timeline lines disappear, the points show as unheard (🎧), a selected point whose record is gone is deselected, non-silent records survive; re-listening returns the real reading and costs a listen. 『もう一度この夜へ』 restarts the same case. The specific one-line hint is gated on the second FAILED NIGHT or two misses in one night.",
  "answer_leak": "PASS — LeakTraceGame.tsx reads game state only through publicView()/readingAt()/restartSameCase(); the QA harness asserts at source level that the component never reads s.c, heard(s), .leak or .house properties (FLOW.leak constant excepted) and uses revealLeak exactly once (post-hit dawn screen), and at logic level that the public view after closing the leak segment vs another segment differs ONLY in the meter number (deep equality with flow/closed stripped). The silent state is identical at every point of a closed segment (level 0 / silent), so it carries no information beyond the meter; dropping a silent record on reopen reveals nothing (it happens for every point of the reopened segment alike, whoever reopens). Valve flash is the same for every segment; segment dimming follows only the focus toggle; house-window blink follows the reading continuity and only while that segment is open.",
  "brute_force": "PASS — budgets enforced by leakLogic (3rd close refused, 6th listen refused, duplicate listen refused, report of an unheard / silent / already-missed point refused, 2 reports max, second report locked until a non-silent new listen after the miss). gameplay-qa-leak.mjs 52/52 incl. the all-plays invariant (a silent reading exists only on the currently closed segment; 0 violations).",
  "visual_affordance": "PASS — glowing 🎧 points read as listen-here; valve lids read as close; the closed lid shows 『閉』 with 『▲開ける（0回）』 beneath (free reopen); 区間◯を調べる labels read as hypothesis buttons; heard points show ▮ bars (『－』 while their segment is closed), missed points show ✕; the last reading on an open segment is echoed as a waveform strip with ずっと/とぎれる; a silent listen shows the reaction row with only 『静か（水が止まっている）』 (live-verified); a non-silent record's live reaction (row, blink) stops while its segment is closed and returns on reopen (live-verified); record strip scrolls (max-height 34px).",
  "no_manual": "PASS — no explanation screen or instruction line; the first touch is inferable from the glowing points / valve lids / trembling needle; continuity is learned from the waveform + window blink, not text; the reopen affordance is the lid label; the silent state is learned by tapping a point of a closed segment; the crew's restore is shown in the timeline and the dig label, not as an instruction.",
  "job_reveal": "PASS — discovery screen (live 2026-09-09): '🎉 明け方 漏れは、止まった', outcome-neutral discoveryEcho (「漏水点はどこか」を考えたよね …), name 水道の漏水を見つける仕事（漏水調査員）, 📖 しごと図鑑に追加された！, 6 seed chips (針の動きを見る / 音を聴き比べる / にせものの音を見分ける / どこを掘るか決める / 記録を見返す / 特にない) → えらんだ. Q2 cards sourced from fact_sheet.json.",
  "pass": true,
  "evidence": [
    "factory/harness/gameplay-qa-leak.mjs run: 52 passed, 0 failed (2026-09-09; flow-only 0.325)",
    "node factory/projects/leak-detective/design/design-sim.mjs: 8/8 verdict flags true, 306/306, gate 200/200/200/200",
    "npm run build ✓, npm run lint (no findings in leak files)",
    "Browser pane session at 375x812 on the Vite dev server (2026-09-09): silent listen (reaction row text only, no .leak-wave element), silent point selected → '静かな点は根拠にならない（水を戻して聴く）' disabled, valid record heard while open stays reportable after closing that segment, explicit / implicit / crew reopen (silent lines and records gone, points unheard, selection cleared), re-listen returns the real reading, miss → locked → silent listen still locked → non-silent listen unlocked, report while closed → 『修理班が弁Aを開けて水を戻した → 修理班が掘る』 + lids 0 during the dig + 『修理班が水を戻して掘っている…』, miss note + reaction row at 812/812, full path A→2.2 / B→0.2 / reopen / gradient B1..B4 / report B3 → hit → dawn → discovery, r9 live-reaction gating — all DOM reads captured in-session",
    "factory/projects/leak-detective/art-requests/place-leak.qa.json (art-qa PASS)",
    "factory/projects/leak-detective/design/design-review-r9.result.json (design PASS 84 under hd-1/hd-2)",
    "factory/projects/leak-detective/impl-review-r3.result.json (FAIL 68 → this repair)",
    "factory/projects/leak-detective/q1-pipeline.json (hd-1, hd-2, one_time_exception, one_time_exception_hd2, f-1..f-11)"
  ],
  "notes": "The dev-console HMR errors noted in v2 (useGame outside GameStateProvider while editing data modules live) did not recur on fresh navigations in this session. Numbers in this document are taken from the harness run recorded above (impl review r3 MEDIUM: evidence parity).",
  "human_decision_domains": [],
  "revision_note": "v4 (2026-09-09, after implementation review r3): crew restores supply before digging (live-verified), reachable silent reaction row, no-scroll layout with the miss note, evidence numbers regenerated from the harness."
}
