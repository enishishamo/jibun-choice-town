# Real User Feedback — Triage Backlog

One line per feedback_id that reached severity HIGH or BLOCKER. Updated
during triage (see `README.md`), not by the observer.

| feedback_id | date | severity | world/game | candidate_factory_gate | status | notes |
|---|---|---|---|---|---|---|
| e67d8587-189f-4c1d-8825-121a06e5e35d | 2026-09-07 | BLOCKER | null (Home/Map-level, not world-specific) | other (onboarding affordance) | released_fixed | "子どもが、どうやって使ったらいいかわからない". Root cause found: on the region-overview map, the up-to-4 markers Progressive Disclosure (2026-09-05) deliberately keeps visible showed no text label at all (opacity:0 until `.in-focus`, which only applies once a district is already entered) — a fresh child saw only tiny unlabeled flame icons, no hover state on touch. Fix: task `real-user-feedback-onboarding-map-labels`, `src/screens/WorldMapScreen.tsx` + `src/index.css` — new `.label-visible` class shows the label for the SAME already-visible overview markers (no new UI/instruction text), plus a `cam.s`-aware edge-clamp (`--label-shift`, measured via `offsetWidth`) so all 4 labels stay fully on-screen at 375px and wider. Took 4 independent-review rounds (round 1: labels invisible; round 2: shown but 1 clipped off-screen; round 3-4: two successive unit-conversion/measurement-timing bugs in the edge-clamp itself) — round 4 PASSED (score 88, 0 blockers/high). QA PASS, product_identity_impact NONE, released to main. Remaining MEDIUM/LOW (backlog, non-blocking): no real child re-test yet confirming the root cause is actually resolved; edge-clamp doesn't check collisions against other fixed UI (compass, etc.) beyond viewport edges; labels shrink slightly with camera zoom on some screens. |
